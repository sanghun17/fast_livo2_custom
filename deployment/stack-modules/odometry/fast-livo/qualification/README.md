# VIO preflight provenance

The optional `flight_safety/vio_shadow_preflight` runtime check reads these two
files. They are byte-identical to the candidate overlay and build manifest moved
out of `tools/fastlivo` during the 2026-09-21 cleanup. They are retained here as
runtime validation inputs; the full campaign and analysis tools are archived.

Relocation does not qualify or enable the candidate. Existing preflight gates,
expected hashes and unqualified defaults remain unchanged.
